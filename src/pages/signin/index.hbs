{{> header pageTitle="Авторизация"}}

<main>
    <div class="content">
        <h1 class="page-title">Авторизация</h1>
        <form class="form-content">
            {{> field/input inputType="text" inputName="login" inputPlaceholder="Логин"}}
            {{> field/input inputType="password" inputName="password" inputPlaceholder="Пароль"}}
            {{> button/blue buttonLink="../main/index.hbs" buttonText="Войти"}}
        </form>
        <div class="form-alternative">
            <p>Ещё нет аккаунта?</p>
            <a href="../signup/index.hbs">Зарегистрироваться</a>
        </div>
    </div>
</main>

{{> footer}}
